Посилання на discord - https://discord.gg/Ytw3SGDy

1. + Створіть структуру даних (масив питань) яка зберігатиме ваші питання. 
    Кожне питання містить 4 варіанти відповіді, та інформація, яка відповідь з них правильна
2. Напишіть код, який виводить питання та 4 кнопки відповіді на екран
  * `querySelector`
  * `textContent` 
3. Напишіть код, який виводить таймер, що займається відліком від 20 до 0
  * `setInterval` / `clearInterval`
  * розуміння що функції можна передавати як аргументи
4. Напишіть код, який після того як таймер досяг 0 виводить наступне питання та знову запускає таймер
5. Напишіть код, який коли натискаєш на кнопку підсвічує обрану кнопку з відповіддю
  * `addEventListener` 
  * викорастання CSS у javascript (шляхом використання `classList`)
6. Напишіть код, який дозволяє змінити свою відповідь доки таймер не досяг нуля
7. Напишіть код, який по завершенню таймера підсвічує правильну відповідь і запускає таймер до наступного запитання (10 секунд)
  * `clearInterval`
  * CSS
8. Напишіть код, що запам'ятовує на яке питання яку відповідь дав користувач і по завершенню всіх питань виводить кількість правильних відповідей
  * робота з об'єктами чи масивами
  * пробігти по фінальному об'єкту чи масиву щоб обрахувати результат
9. Напишіть код, який записує коли користувач натиснув кнопку (умовно "через 2.5" секунди після початку відліку). 
Оновіть формулу нарахування очок за логікою: за неправильну відповідь 0, за правильну (20-час реагування+0.5)*50 але не більше 1000
   * `Date` для отримання поточного часу
10. Напишіть код, що виводить кількість очок в кінці та починає розпочати процес з нуля (кнопка). 
Додайте ще декілька питань (умовні 20) і у грі виводьте довільні 5 з них
  * `Math.random()` для отримання довільних значень